# dependencies
node_modules
.pnpm-store

# nuxt / nitro build output
.nuxt
.output
.data
dist

# environment — never commit real values
.env
.env.*
!.env.example

# hls packaging run: work dir, manifests, staged clips
.hls-work

# local tooling
.DS_Store
*.log
*.tsbuildinfo
.vitest
coverage
.eslintcache

# docker volumes mounted into the repo (none today; guard anyway)
docker/data
